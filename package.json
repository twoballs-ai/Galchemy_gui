{
  "name": "engine",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w @engine/editor",
    "build": "npm run build -w @engine/editor",
    "preview": "npm run preview -w @engine/editor",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint ."
  }
}